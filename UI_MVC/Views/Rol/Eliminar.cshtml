@model Tranferencia_Datos.Rol_DTOs.Obtener_PorID

@{
    ViewData["Title"] = "ELIMINAR";
}

<style>
    /* Estilos personalizados */
    .form-container {
        max-width: 550px;
        padding: 20px;
        background-color: #f7f7f7;
        border-radius: 20px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        margin: auto;
        margin-top: 20px;
    }

        .form-container h1 {
            text-align: center;
            font-size: 28px;
            font-weight: bold;
            color: #333;
            text-transform: uppercase;
        }

        .form-container .row {
            margin-bottom: 10px;
        }

        .form-container label {
            font-weight: bold;
        }

</style>

<!-- Cuerpo HTML -->
<div class="form-container">
    <h1>Eliminar</h1>

    <div>
        <h5>Datos Del Rol</h5>
        <hr />

        <!-- Fila #1 -->
        <div class="row">

            <div class="col-sm-6">
                <dt>NOMBRE:</dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.Nombre)
                </dd>
            </div>

        </div>

    </div>

    <!-- BOTONES -->
    <form asp-action="Eliminar">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="ELIMINAR" class="btn btn-danger" /> |
        <a asp-action="Registrados" class="btn btn-secondary">CANCELAR</a>
    </form>

</div>